name: boiforms
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  riverpod: ^2.5.1
  intl: ^0.19.0
  pdf: ^3.11.3
  excel: ^2.1.0
  file_picker: ^10.3.3
  csv: ^5.1.1
  open_filex: ^4.7.0
  path_provider: ^2.1.5
  printing: ^5.12.0  # Added for PdfPreview
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets section for fonts
  assets:
    - assets/fonts/